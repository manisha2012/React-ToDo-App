var expect = require('expect');

// Describe is strictly a visual function. It formats our output to be more readable. We can breakup our tests into group of tests
describe('App', () => {
  it('should properly run tests', () => {
    expect(1).toBe(1);
  });
});
